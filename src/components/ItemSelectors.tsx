import React, { useState } from 'react';
import { TPizza } from '../redux/pizzaSlice';

const ItemSelectors: React.FC<{ pizza: TPizza }> = ({ pizza }) => {
  return <></>;
};

export default ItemSelectors;
